numpy>=1.26
scipy>=1.11
sentence-transformers>=3.0
anthropic>=0.34
faiss-cpu>=1.8          # content_filter ANN sweep: IVF, HNSW and LSH all come from faiss
implicit>=0.7           # collab_filter: reference ALS/BPR, used only to check the hand-rolled ones
rank_bm25>=0.2          # content_filter: lexical baseline + hybrid RRF fusion
datasets>=2.14          # content_filter: finetune_retriever.py's training set format
accelerate>=1.1         # content_filter: required by SentenceTransformerTrainer
